.DS_Store

# generated by the Claude Design self-check; regenerated remotely
design-system/_ds_manifest.json

# a separate project that happens to live inside this folder — it has its
# own git repo, so without this git offers to swallow it as a submodule
athenadarling/
